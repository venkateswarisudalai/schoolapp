# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Android build files
android/.gradle
android/app/build
android/build
android/capacitor-cordova-android-plugins/build

# Environment
.env

# Keystore
android/keystore/*.jks
android/app/keystore/*.jks

# Firebase Admin SDK service account key (never commit)
firebase-service-account.json

# Exported student/parent login credentials (never commit)
*_credentials.csv

# Signed release binaries (39 MB of .aab/.apk — publish via Play Console, not git)
releases/

# One-shot import scripts: these embed real student/parent PII
# (names, DOB, blood group, home address, phone, medical notes). Never commit.
scripts/import*.js

# Local backup copies
*.OLD.*
*.PREVIOUS.*
